#This is going to be the first properties that will be read
#First is read the config server and then the microservice will be registered in eureka as a client
spring.application.name=products-service
spring.profiles.active=dev
spring.cloud.config.uri=http://localhost:8888